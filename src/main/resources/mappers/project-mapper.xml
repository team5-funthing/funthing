<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProjectDAO">

	<select id="getProject" parameterType="project" resultType="project">
		SELECT * 
		FROM project
		WHERE projectno = #{projectNo}
	</select>
	
<!-- 
	<resultMap type="#modelname" id="YourResultSet">
		<id property="param1" column="columnname1" />
		<result property="param2" column="columnname2" />
		<result property="param3" column="columnname3" />
	</resultMap>


	<select id="getProjectList" resultMap="YourResultSet">
		
	</select>
	
-->

	<insert id="insertProject" parameterType="project">
		

		<if test="projectNo == -1">
			INSERT INTO project(projectNo, creator, email, projectTitle, goalMoney, category, projectSubTitle, projectstory, projectdetail, projectcaution, status, informationagree, startdate, enddate, writestatus)
			VALUES ((SELECT nvl(max(projectNo), 0) + 1 from project), #{creator}, #{email}, #{projectTitle}, #{goalMoney}, #{category}, #{projectSubTitle}, #{projectStory}, #{projectDetail}, #{projectCaution}, #{status}, #{informationAgree}, #{startDate}, #{endDate}, #{writeStatus})
		</if>
		<if test="projectNo != -1">
			INSERT INTO project(creator, projecttitle, goalmoney, category, projectsubtitle, projectstory, projectdetail, projectcaution, status, informationagree, startdate, enddate, writestatus)
			VALUES (#{creator}, #{projectTitle}, #{goalMoney}, #{category}, #{projectSubTitle}, #{projectStory}, #{projectDetail}, #{projectCaution}, #{status}, #{informationAgree}, #{startDate}, #{endDate}, #{writeStatus})
		</if>
		
		
				
	</insert>
	
<!--
	<update id="updateSomething" parameterType="#modelname">
		UPDATE tablename1
		SET
		columnname1=#{param1}
		WHERE
		columnname2 = #{param2}
	</update>

	<delete id="deleteSomething" parameterType="int">
		DELETE FROM tablename1 WHERE
		columnname1 = #{param1}
	</delete>
 -->

</mapper>
